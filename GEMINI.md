# GEMINI.md — Metamorph Guidelines for Antigravity & Gemini

> This document complements and synchronizes with [AGENTS.md](./AGENTS.md). Both are automatically loaded by Antigravity CLI and IDE upon workspace initialization.

Refer to [AGENTS.md](./AGENTS.md) for:
- Metamorph Architectural Invariants (Shadow Workspace Zero-Risk, Mozaik v4 Event Bus, Hexagonal Purity).
- Monorepo directory structure (`packages/@nikelyh/*`, `apps/dashboard`).
- Verification commands (`npm run build`, `npm run typecheck`, `npm run test`).
- Agent swarm conventions, ephemeral participant lifecycle (`leave()`), and concurrency bounds.
- Deprecations and critical warnings (`resolveMigrationCatalog`, App Router layouts, Windows terminal CWD).
- Pull Request Templates Suite (`.github/PULL_REQUEST_TEMPLATE/`) and Git Flow conventions.
- Clean Code, SOLID principles, and functional purity standards (`.agents/rules/clean_code.md`).
