interface FolderPickerProps {
    value: string;
    onChange: (path: string) => void;
}
export declare const FolderPicker: ({ value, onChange }: FolderPickerProps) => import("react").JSX.Element;
export {};
